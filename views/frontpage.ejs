<!DOCTYPE html>
<html lang="en">
<head>
    <title>Movie Recommendation</title>
    <style>

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        
        .header {
            background: linear-gradient(to right, #4F46E5, #A683E3);
            padding: 20px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 32px;
            font-weight: bold;
            color: white;
            margin: 0;
        }
        
        .hero {
            background-image: url('/static/login_background.jpg');
            background-size: cover;
            height: 700px;
            background-position: center;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
        }
        
        .hero h1 {
            font-size: 48px;
            font-weight: bold;
            margin-bottom: 16px;
            animation: fadeInDown 1s ease;
        }
        
        .hero p {
            font-size: 24px;
            margin-bottom: 24px;
            animation: fadeInUp 1s ease;
        }
        
        .btn-primary {
            background-color: #39A2DB;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            animation: fadeIn 1s ease;
        }
        
        .btn-primary:hover {
            background-color: #1C6E8C;
        }
        
        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
        
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <h1>Popcorn Preferences</h1>
    </header>

    <section class="hero">
        <div class="container">
            <h1>Discover Your Perfect Movie</h1>
            <p>Get personalized movie recommendations based on your preferences.</p>
            <button id="loginButton" class="btn-primary">Get Started</button>
        </div>
    </section>
    
    <script>
        document.getElementById("loginButton").addEventListener("click", function() {
            window.location.href = "/home";
        });
    </script>
</body>
</html>
